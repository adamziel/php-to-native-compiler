phpc reports a structured runtime boundary for ArrayAccess reference sources whose offsetGet() returns by value. PHP behavior can emit engine-specific indirect-modification notices, so this fixture is intentionally not byte-for-byte compared against system PHP.
