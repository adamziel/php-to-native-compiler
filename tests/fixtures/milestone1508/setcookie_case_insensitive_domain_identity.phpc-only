System PHP CLI does not expose phpc's deterministic in-process CLI header log through headers_list(); this fixture documents bounded case-insensitive domain identity matching for setcookie()/setrawcookie() replacement.
