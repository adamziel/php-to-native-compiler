This fixture exercises a WordPress-shaped `wpdb` method that reaches phpc's
MySQLi statement long-parameter handle validation boundary. System PHP would
require a real mysqli statement object; phpc now validates that
`mysqli_stmt_send_long_data()` receives an active placeholder statement before
any deterministic chunk state can be recorded.
