System PHP CLI does not expose phpc's deterministic in-process CLI header log through headers_list(); this fixture documents bounded private/private_no_expire/public session cache limiter header variants with a synthetic fixed request timestamp.
