Uses deterministic mysqli wp_options placeholder state and bounded option_name LIKE prefix scans with ORDER BY option_name rather than a real MySQL database, general SQL ordering, full WordPress option APIs, persistent object cache, or native DB lowering.
