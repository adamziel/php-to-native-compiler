Project-specific database boundary: phpc returns deterministic mysqli_result placeholders and clean connection diagnostics for the reached WordPress SHOW FULL COLUMNS and DESCRIBE metadata queries, including the current bounded wp_options schema rows, while system PHP depends on a real host MySQL connection, schema metadata, and result resources.
