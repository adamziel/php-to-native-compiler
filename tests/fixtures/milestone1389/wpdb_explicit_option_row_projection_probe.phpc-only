Uses deterministic mysqli wp_options placeholder state and exact explicit option-row projection fetching rather than a real MySQL database, arbitrary SQL, persistent WordPress option APIs, or native DB lowering.
