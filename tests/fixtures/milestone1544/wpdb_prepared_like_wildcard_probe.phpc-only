Uses deterministic mysqli wp_options placeholder state and bounded prepared option_name LIKE wildcard scans rather than a real MySQL database, prepared ESCAPE clauses, SQL-mode-aware option reads, collation fidelity, persistent object cache/transients, full WordPress option APIs, or native DB lowering.
