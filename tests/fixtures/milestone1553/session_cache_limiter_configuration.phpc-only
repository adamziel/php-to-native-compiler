System PHP CLI does not expose phpc's deterministic in-process CLI header log through headers_list(); this fixture documents bounded session_cache_limiter()/session_cache_expire() configuration for phpc's request-local session cache-header state.
