This fixture exercises phpc's deterministic placeholder `mysqli_kill()`
thread-id boundary through a WordPress-shaped `wpdb` connection thread
lifecycle method. System PHP needs a real mysqli connection and host server
thread state; phpc only records local bookkeeping around placeholder
thread-id kill acceptance and leaves the placeholder connection open.
