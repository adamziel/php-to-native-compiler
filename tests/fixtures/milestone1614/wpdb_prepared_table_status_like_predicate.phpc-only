Uses phpc's deterministic in-memory MySQLi schema-state island for bounded prepared SHOW TABLE STATUS WHERE Name LIKE metadata predicates; system PHP needs a real database connection and is not comparable.
