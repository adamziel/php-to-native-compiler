This fixture exercises phpc's deterministic placeholder `mysqli_debug()`
DBUG-configuration metadata through a WordPress-shaped `wpdb` connection
diagnostics method. System PHP configures MySQL DBUG tracing through the
client library; phpc returns deterministic success without parsing DBUG
options, creating trace files, mutating host client-library debug state,
touching sockets, or querying database state.
