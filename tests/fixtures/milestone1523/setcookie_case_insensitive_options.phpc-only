System PHP CLI does not expose phpc's deterministic in-process CLI header log through headers_list(); this fixture documents bounded case-insensitive options-array key matching for setcookie()/setrawcookie().
