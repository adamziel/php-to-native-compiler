This fixture exercises a WordPress-shaped `wpdb` method that reaches phpc's
explicit MySQLi statement warning-count diagnostic boundary. System PHP would
require a real mysqli statement object, statement warning tracking, and
diagnostic state; phpc only records the reached
`mysqli_stmt_warning_count()` boundary with a stable unsupported diagnostic and
does not implement statement objects or host database state.
