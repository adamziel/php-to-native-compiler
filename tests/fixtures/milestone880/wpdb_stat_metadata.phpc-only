This fixture exercises phpc's deterministic `mysqli_stat()` placeholder
through a WordPress-shaped `wpdb` server-status metadata path. System PHP
would require the mysqli extension and a real host connection to provide
actual server counters.
