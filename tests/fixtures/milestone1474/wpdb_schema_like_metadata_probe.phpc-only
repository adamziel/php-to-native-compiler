Uses phpc's deterministic in-memory MySQLi schema-state island for wildcard SHOW TABLES, SHOW TABLE STATUS, and SHOW COLUMNS metadata; system PHP needs a real database connection and is not comparable.
