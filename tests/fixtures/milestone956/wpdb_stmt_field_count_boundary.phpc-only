This fixture exercises phpc's explicit MySQLi statement field-count boundary
through a WordPress-shaped `wpdb` method. System PHP would require a real
mysqli statement object, statement result metadata, and field-count state;
phpc only records the reached `mysqli_stmt_field_count()` boundary with a
stable unsupported diagnostic and does not implement host database state.
