This fixture exercises phpc's deterministic placeholder `mysqli_kill()`
thread-id boundary after placeholder MySQLi setup. System PHP needs a real
mysqli connection and host server thread state; phpc only accepts the
deterministic placeholder `mysqli_thread_id()` value and does not kill,
invalidate, reconnect, or mutate a real connection.
