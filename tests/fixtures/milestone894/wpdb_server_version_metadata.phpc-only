This fixture exercises phpc's deterministic placeholder
`mysqli_get_server_version()` metadata through a WordPress-shaped `wpdb`
connection-metadata bookkeeping path. System PHP needs a real connection and
reports the actual server version.
