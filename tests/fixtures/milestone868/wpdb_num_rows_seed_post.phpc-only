This fixture uses phpc's deterministic placeholder MySQLi result state for
WordPress-shaped `wpdb` result-count paths. System PHP would require a real
MySQL server and WordPress schema with matching rows.
