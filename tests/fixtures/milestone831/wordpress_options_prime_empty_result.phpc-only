Project-specific database boundary: phpc returns deterministic empty mysqli_result placeholders and clean connection diagnostics for the reached WordPress option-name cache priming SELECT, while system PHP depends on a real host MySQL connection, result resources, and database contents.
