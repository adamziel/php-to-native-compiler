System PHP CLI does not expose phpc's deterministic in-process CLI header log through headers_list(); this fixture documents bounded last-value handling for duplicate differently cased setcookie()/setrawcookie() options-array keys.
