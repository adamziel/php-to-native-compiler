This fixture exercises a WordPress-shaped `wpdb` method that reaches phpc's
explicit MySQLi statement attribute-read boundary. System PHP would require a
real mysqli statement object, statement attribute catalog, and option registry
state; phpc only records the reached `mysqli_stmt_attr_get()` boundary with a
stable unsupported diagnostic and does not implement statement objects or host
database state.
