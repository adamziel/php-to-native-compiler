Uses deterministic mysqli wp_options placeholder state and bounded prepared option_name LIKE prefix scans for transient-shaped rows rather than a real MySQL database, SQL LIKE engine, or persistent WordPress object cache.
