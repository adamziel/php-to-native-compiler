Uses phpc's deterministic in-memory MySQLi schema-state island for bounded literal SHOW TABLE STATUS WHERE Name IN metadata predicates; system PHP needs a real database connection and is not comparable.
