This fixture exercises phpc's deterministic placeholder `mysqli_commit()` and
`mysqli_rollback()` boundaries. System PHP would require the mysqli extension
and a real host connection to commit or roll back actual transaction state.
