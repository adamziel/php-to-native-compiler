# Developer 286 Capacity Triage

Generated by `developer-286` on 2026-06-05 from:

- harness DB: `/home/claude/php-to-native-compiler/.harness/harness.sqlite3`
- worktree: `/home/claude/php-to-native-compiler/.harness/worktrees/developer-286`
- branch: `work/developer-286`
- source head: `6899bf49`

Scope: reserve-capacity triage only. No compiler/runtime/product source edits,
no PHPT gate, and no public score movement.

## Decision

`developer-286` did not claim a source or replay lane because the harness had
no queued Developer lane and no queued direct assignment for this agent at the
time of inspection. The active M0/M1 broadcasts and lane notes direct
unassigned developers away from unowned compiler/runtime edits while the team
stabilizes PHPT regression measurement and harness control-plane failures.

The deterministic action taken here is to leave `developer-286` available as
reserve capacity and record the exact evidence needed by a Manager or
Integrator to assign it without duplicating another owner's work.

## Measured State

| Item | Value |
| --- | ---: |
| queued work lanes | 0 |
| in-progress work lanes | 28 |
| open bug reports | 6 |
| active Developer rows | 82 |
| active Developer rows still at placeholder `running` | 15 |
| queued direct messages for `developer-286` | 0 |

The in-progress lane ids observed were:

`118, 117, 116, 115, 114, 113, 112, 111, 110, 109, 108, 107, 106, 100, 95, 89, 87, 86, 85, 83, 82, 81, 78, 74, 73, 69, 68, 8`

The newest active failed-test lane was lane 118, already claimed by
`developer-274`, for test run 127's recurring `python -m unittest discover -s
tests -v` command-selection failure. The canonical control-plane repair remains
lane 8 / lane 100 ownership rather than a duplicate compiler/runtime lane.

The public metric remains the PLAN-pinned accepted score:

`7873 / 20294 = 38.79%`

The blocked `221205Z` candidate remains non-public progress:

`7197 / 20294 = 35.46%` with `1166` latest-public PASS regressions.

## Missing Inputs

- `DEVELOPMENT.md` is absent under `/home/claude/php-to-native-compiler`.
- `PLAN.md` is absent in this worktree but present in the parent checkout at
  `/home/claude/php-to-native-compiler/PLAN.md`; the metric and late-priority
  rules above were read from that file.
- The requested SQLite MCP wrappers and `code_search` tool were not exposed in
  this session. Python's standard `sqlite3` module and `rg`/shell reads were
  used instead.

## Next Deterministic Action

Assign `developer-286` to one explicit lane before source edits. Good fits are:

- a narrowly owned M0/M1 artifact lane that does not overlap active report
  lanes 106-118;
- a handoff from a stale/non-live lane owner after the Manager updates
  `work_lanes.branch` and `work_lanes.worktree`;
- a source lane only after M0 control-plane blockers are resolved and the lane
  names owned files, focused tests, CLI exercise path, docs, and unsupported
  edge cases.

Do not use this branch for self-selected runtime/database/COW changes while the
current M0 broadcasts remain active.

## Commands Run

```sh
sed -n '1,220p' AGENTS.md
sed -n '1,260p' docs/PROGRESS.md
sed -n '1,260p' docs/ARCHITECTURE.md
sed -n '1,240p' docs/SUPPORT.md
sed -n '1,240p' README.md
sed -n '1,240p' docs/LOOP_MEMORY.md
rg --files -g 'DEVELOPMENT.md' -g 'GOAL.MD' -g 'docs/NEXT_TASKS.md' -g 'docs/OPERATIONS.md'
find /home/claude/php-to-native-compiler -maxdepth 5 -name DEVELOPMENT.md -print
sed -n '1,220p' GOAL.MD
sed -n '1,260p' docs/NEXT_TASKS.md
sed -n '1,260p' docs/OPERATIONS.md
sed -n '1,220p' /home/claude/php-to-native-compiler/PLAN.md
git status --short --branch
git rev-parse --short HEAD
```

```sh
python3 - <<'PY'
import sqlite3
con = sqlite3.connect('/home/claude/php-to-native-compiler/.harness/harness.sqlite3')
queries = {
    'queued_lanes': "select count(*) from work_lanes where status='queued'",
    'in_progress_lanes': "select count(*) from work_lanes where status='in_progress'",
    'open_bugs': "select count(*) from bug_reports where status='open'",
    'active_devs': "select count(*) from agents where role='Developer' and ended_at is null and current_status not like 'stopped%'",
    'running_placeholder_devs': "select count(*) from agents where role='Developer' and ended_at is null and current_status='running'",
    'dev286_messages': "select count(*) from messages where target='developer-286' and status='queued'",
}
for name, sql in queries.items():
    print(name, con.execute(sql).fetchone()[0])
PY
```
