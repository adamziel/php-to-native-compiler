# 221205Z SPL Regression Shard Report

Diagnostic/control-plane report for lane 18. This report only classifies
existing PHPT artifacts for the blocked 221205Z candidate and cannot move the
public PHPT score.

## Inputs

- Candidate evidence:
  `/home/claude/supervised-php-compiler/state/logs/phpt-full-current-score-20260604T221205Z-php-src-f97ff59-public-56fe9377-source-56fe9377`
- Accepted baseline evidence referenced by the candidate preflight:
  `/home/claude/supervised-php-compiler/state/logs/phpt-full-current-score-20260604T135138Z-php-src-f97ff59-public-0b917f67-source-0b917f67`
- Baseline public score: 7873/20294 at `0b917f67`
- Candidate public score: 7197/20294 at `56fe9377`
- Candidate PASS-regression set:
  `regressions-from-latest-published-passes.txt`

## Exact Count

`regressions-from-latest-published-passes.txt` contains 1166 latest-public
PASS regressions. Of those, exactly 137 rows start with `php-src/ext/spl/`.

## Cluster Breakdown

| Count | SPL class/subsystem | Row examples |
| ---: | --- | --- |
| 29 | ArrayObject / ArrayIterator | `ArrayObject/array_001.phpt`, `ArrayObject/array_006.phpt`, `ArrayObject/array_007.phpt`, `ArrayObject/arrayObject_asort_basic1.phpt` |
| 21 | Other SPL core functions/classes | `class_implements_basic2.phpt`, `class_uses_variation.phpt`, `dit_005.phpt`, `dllist_005.phpt`, `fixedarray_001.phpt` |
| 19 | SplFileObject / file iterators | `SplFileObject/bug60201.phpt`, `SplFileObject/bug68479.phpt`, `SplFileObject/SplFileObject_fgetcsv_basic.phpt` |
| 18 | Iterator / RecursiveIterator utilities | `DirectoryIterator_getBasename_basic_test.phpt`, `iterator_005.phpt`, `regexiterator_getregex.phpt`, `spl_iterator_to_array_error.phpt` |
| 18 | Root SPL legacy bug tests | `bug28822.phpt`, `bug31185.phpt`, `bug40036.phpt`, `gh18421.phpt` |
| 9 | SplFixedArray | `SplFixedArray_change_size_during_iteration.phpt`, `SplFixedArray__construct_param_array.phpt`, `splfixedarray_offsetExists_larger.phpt` |
| 9 | SplObjectStorage | `SplObjectStorage/bug53144.phpt`, `SplObjectStorage/bug69227.phpt`, `SplObjectStorage/SplObjectStorage_seek.phpt` |
| 7 | SPL autoloading | `autoloading/bug61697.phpt`, `autoloading/spl_autoload_003.phpt`, `autoloading/spl_autoload_call_basic.phpt` |
| 7 | SPL data structures | `SplDoublyLinkedList_bottom_empty.phpt`, `SplDoublyLinkedList_offsetGet_param_array.phpt`, `SplDoublyLinkedList_offsetUnset_negative-parameter.phpt` |

## Status And Symptoms

For the 137 SPL regression rows:

| Evidence source | Status breakdown |
| --- | --- |
| Accepted `current-status.normalized.tsv` | 137 `PASSED` |
| Accepted `all-results.txt` | 137 `PASSED` |
| Candidate `current-status.normalized.tsv` | 137 missing |
| Candidate `all-results.txt` | 137 missing |

The candidate did run other SPL rows, but none of the 137 regression rows. The
candidate aggregate contains 529 `php-src/ext/spl/` rows from shards 01, 02,
05, and 06: 271 `PASSED`, 257 `FAILED`, and 1 `SKIPPED`. Shards 03 and 04
contain zero SPL rows.

Shard-level symptoms:

- Candidate `shard-03/results.txt` stops at 2114 rows in `ext/pdo_mysql`.
  `shard-03/run-tests.log` is absent. `shard-03/stdout.log` reports:
  `ERROR: cannot open directory:
  /tmp/phpt-full-current-score-20260604T221205Z-php-src-f97ff59-public-56fe9377-source-56fe9377/run-tests-harnesses/shard-03/ext/pdo/tests`.
- Candidate `shard-04/results.txt` stops at 2268 rows in `ext/pdo_pgsql`.
  `shard-04/run-tests.log` is absent. `shard-04/stdout.log` reports:
  `ERROR: cannot open directory:
  /tmp/phpt-full-current-score-20260604T221205Z-php-src-f97ff59-public-56fe9377-source-56fe9377/run-tests-harnesses/shard-04/ext/pdo/tests`.
- `shard-exit-codes.tsv` records exit code `1` for all six shards, but only
  shards 03 and 04 are truncated before SPL coverage. The signal summary has
  no signal exits.

The candidate evidence directory does not retain `shard-*.tests`, and the
candidate `/tmp` run root is gone, so exact candidate row-list membership
cannot be read directly. However, the accepted 12-shard baseline places these
137 PASS rows only in baseline shards 03, 04, 09, and 10:

| Accepted baseline shard bucket | Rows |
| --- | ---: |
| shard-03 | 32 |
| shard-04 | 39 |
| shard-09 | 35 |
| shard-10 | 31 |

Using the gate's 12-to-6 shard collapse, this corresponds to 67 rows in the
candidate shard-03 bucket and 70 rows in the candidate shard-04 bucket. That
matches the observed candidate symptom: shard-03 and shard-04 abort before
SPL, and all 137 SPL latest-public PASS rows are absent from candidate status.

## Likely Buckets

- Harness/control-plane: 137/137 SPL regressions are best classified as
  unobserved rows caused by candidate shard truncation, not as proven row-level
  compiler/runtime semantic regressions.
- Semantic unknown: ArrayObject/ArrayIterator, SplFileObject, SplFixedArray,
  SplObjectStorage, SPL autoloading, Iterator/RecursiveIterator, SPL data
  structures, root SPL bug tests, and other SPL core-function behavior remain
  named unknowns for these rows because the candidate did not emit row-level
  output for them.
- Adjacent semantic debt: the candidate has 257 failed SPL rows outside this
  latest-public PASS regression set. Those failures may still indicate real
  unsupported SPL semantics, but they are not the 137 PASS regressions counted
  for this lane.

## Representative Rows

| Row | Accepted status | Candidate status | Diagnostic notes |
| --- | --- | --- | --- |
| `php-src/ext/spl/tests/ArrayObject/array_006.phpt` | `PASSED` in accepted `current-status` and `all-results` | Missing from candidate `current-status` and `all-results` | Accepted shard-03 PASS. Inferred candidate shard-03 bucket; candidate shard-03 aborts in `ext/pdo_mysql` before SPL and has no `run-tests.log`. |
| `php-src/ext/spl/tests/autoloading/bug61697.phpt` | `PASSED` in accepted `current-status` and `all-results` | Missing from candidate `current-status` and `all-results` | Accepted shard-04 PASS. Inferred candidate shard-04 bucket; candidate shard-04 aborts in `ext/pdo_pgsql` before SPL and has no `run-tests.log`. |
| `php-src/ext/spl/tests/SplFileObject/bug68479.phpt` | `PASSED` in accepted `current-status` and `all-results` | Missing from candidate `current-status` and `all-results` | Accepted shard-09 PASS. Inferred candidate shard-03 bucket under the 12-to-6 collapse. No candidate row diagnostics exist. |
| `php-src/ext/spl/tests/SplFixedArray__construct_param_array.phpt` | `PASSED` in accepted `current-status` and `all-results` | Missing from candidate `current-status` and `all-results` | Accepted shard-10 PASS. Inferred candidate shard-04 bucket under the 12-to-6 collapse. No candidate row diagnostics exist. |
| `php-src/ext/spl/tests/SplObjectStorage/bug69227.phpt` | `PASSED` in accepted `current-status` and `all-results` | Missing from candidate `current-status` and `all-results` | Accepted shard-09 PASS. Inferred candidate shard-03 bucket under the 12-to-6 collapse. No candidate row diagnostics exist. |

## Focused Replay Recommendations

Do not run a full PHPT gate for this report. Use only the saved candidate
`run_gate.sh` and `environment.txt` values if replay is requested by the
manager or integrator.

Recommended deterministic order:

1. First repair or bypass the harness-copy issue that made
   `run-tests-harnesses/shard-03/ext/pdo/tests` and
   `run-tests-harnesses/shard-04/ext/pdo/tests` unavailable. Without that,
   a replay can reproduce the same truncation without testing SPL semantics.
2. Reconstruct a tiny replay list from the five representative rows above and
   run it through the same command shape recorded in `run_gate.sh`: `php
   run-tests.php -q -n -p <wrapper> -r <row-list> -W <results> -s <log>
   --no-color --set-timeout 65 --temp-source <php-src> --temp-target
   <temp>`. Source the wrapper path, PHPC binary, timeout values, php-src pin,
   and source/public heads from the saved `environment.txt`.
3. Add one control SPL row already observed in the candidate, such as
   `php-src/ext/spl/tests/ArrayObject/arrayObject___construct_basic4.phpt`,
   so replay output proves the focused harness path is recording rows.
4. If the five representatives emit row-level statuses, replay the full
   137-row SPL list extracted with `grep '^php-src/ext/spl/'
   regressions-from-latest-published-passes.txt` into a lane-local evidence
   directory. Keep it separate from public score artifacts.
5. Only after a clean focused replay should these rows be considered for a new
   public score gate. This report itself is diagnostic/control-plane work only.
