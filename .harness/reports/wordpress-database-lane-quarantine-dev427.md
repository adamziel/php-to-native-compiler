# WordPress Database Lane Quarantine

Generated by `developer-427` at `2026-06-05T10:01:59Z`.

Lane: `work_lanes#114`

Scope: read-only quarantine/proposal. No compiler/runtime/product source files
were edited in `work/developer-427`. No full PHPT gate was run. No public score
movement is claimed.

## Decision

Do not continue or integrate the self-selected WordPress/database slice from
`developer-233` as M0/M1 progress.

The visible prior work is a dirty, uncommitted product-source slice in
`/home/claude/php-to-native-compiler/.harness/worktrees/developer-233`. It may
be useful future context, but it was started without an active assigned source
lane while current manager guidance was prioritizing M0 PASS-regression triage
and M1 measurement/control-plane work. Preserve it for audit; do not revert it,
do not merge it, and do not build a new claim from it in this lane.

`developer-298`, the intermediate reassignment target, had a clean worktree in
the direct check below and did not produce a visible WordPress/database report
artifact in the checked path.

## Prior Lane Context

Relevant scheduler-visible state:

| Item | Evidence |
| --- | --- |
| Self-selected slice started | `events#93748`: `developer-233` claimed a narrow unowned runtime/database slice for prepared `wp_options` autoload readback. |
| Product edits began | `events#93861`: `developer-233` reported implementing exact backticked prepared `wp_options` `option_value`/`autoload` projection and starting focused validation. |
| Quarantine lane assigned | `events#93810`: manager assigned lane 114 to `developer-233` as read-only, no source edits, no full PHPT gate, no public score movement. |
| Stop instruction delivered | `events#93838`: queued message told `developer-233` to use lane 114 as authoritative and produce the quarantine artifact. |
| Reassignment | `events#94098`: manager reassigned lane 114 to `developer-298`. |
| Current assignment | `work_lanes#114`: manager-24 assigned lane 114 to `developer-427` with artifact `.harness/reports/wordpress-database-lane-quarantine-dev427.md`. |

`agents` rows checked:

| Agent | Status | Worktree |
| --- | --- | --- |
| `developer-233` | `stopped`, `ended_at=2026-06-05T09:28:19+00:00` | `/home/claude/php-to-native-compiler/.harness/worktrees/developer-233` |
| `developer-298` | `stopped`, `ended_at=2026-06-05T09:28:19+00:00` | `/home/claude/php-to-native-compiler/.harness/worktrees/developer-298` |
| `developer-427` | running on lane 114 | `/home/claude/php-to-native-compiler/.harness/worktrees/developer-427` |

## Visible Dirty Work

Direct status for `developer-233`:

```text
## work/developer-233
 M compiler/src/interpreter.rs
 M compiler/tests/mysqli_extension.rs
?? tests/fixtures/milestone2305/
```

Diff summary:

```text
compiler/src/interpreter.rs        | 6 +++++-
compiler/tests/mysqli_extension.rs | 6 +++++-
2 files changed, 10 insertions(+), 2 deletions(-)
```

The untracked fixture files are:

```text
tests/fixtures/milestone2305/mysqli_prepared_option_value_autoload_backticked.cli
tests/fixtures/milestone2305/mysqli_prepared_option_value_autoload_backticked.exit
tests/fixtures/milestone2305/mysqli_prepared_option_value_autoload_backticked.php
tests/fixtures/milestone2305/mysqli_prepared_option_value_autoload_backticked.phpc-only
tests/fixtures/milestone2305/mysqli_prepared_option_value_autoload_backticked.stderr
tests/fixtures/milestone2305/mysqli_prepared_option_value_autoload_backticked.stdout
```

Observed code intent from the diff:

- `compiler/src/interpreter.rs` extends one exact
  `SELECT option_value, autoload FROM wp_options WHERE option_name = ? LIMIT 1`
  match to also accept
  ``SELECT `option_value`, `autoload` FROM `wp_options` WHERE `option_name` = ? LIMIT 1``.
- `compiler/tests/mysqli_extension.rs` adds one assertion path for that
  backticked equality projection.
- The untracked fixture exercises `mysqli_execute_query()`,
  `mysqli_prepare()`, `mysqli_stmt_execute()`, `mysqli_stmt_get_result()`,
  `mysqli_fetch_assoc()`, `mysqli_num_rows()`, and `mysqli_num_fields()` over
  the deterministic placeholder `wp_options` state island.

This is not feature-complete under project rules. The dirty slice has no
committed docs/progress/support update in the prior worktree, no committed CLI
fixture path in the current branch, no completed scheduler-visible validation
event, and no manager-approved source lane. It also does not address the current
M0 blocker: the blocked `221205Z` full gate remains non-publishable because it
has `1166` latest-public PASS regressions.

Direct status for `developer-298`:

```text
## work/developer-298
```

No dirty paths were reported by `git status --short --branch` for
`developer-298`.

## Existing Documentation Boundary

The current docs already keep the database/WordPress boundary narrow:

- `docs/WORDPRESS_COMPATIBILITY.md` says the WordPress inventory and synthetic
  bootstrap smokes are measurement tools, not a WordPress support claim.
- `docs/extensions/mysqli.md` marks MySQLi as a deterministic placeholder
  boundary. It repeatedly names unsupported host database state, broad SQL,
  real connections, warning/error fidelity, PDO behavior, and native database
  lowering.
- The current `docs/extensions/mysqli.md` already documents exact prepared
  equality projections for non-backticked
  `SELECT option_value, autoload FROM wp_options WHERE option_name = ? LIMIT 1`
  and broader backticked prepared name-list/autoload-list slices. That makes
  the quarantined equality-projection spelling a plausible future small slice,
  but not an urgent M0 repair.

## Future Lane Placement

A future WordPress/database lane should wait until the current M0/M1 gates are
settled:

1. M0: `221205Z` PASS regressions are repaired, replayed cleanly, or
   adjudicated; absent-row and direct FAILED/BORKED evidence is not still
   blocking public-score publication.
2. M1: scheduler/test-loop command selection and stale-agent liveness bugs are
   closed with accepted focused harness evidence, so new source work is not
   confused with control-plane churn.
3. A manager explicitly opens a clean source lane from a current worktree with
   one bounded database target, precheck/postcheck evidence, focused Rust tests,
   a `phpc run` CLI fixture, docs/support/progress updates, and named
   unsupported edges.

If that future lane chooses to reuse this idea, the smallest bounded target
would be "accept exact backticked prepared `wp_options` equality projection for
`option_value`/`autoload` in the deterministic placeholder MySQLi state island."
It must still avoid claiming real MySQL, broad SQL, WordPress support, PDO,
host database state, or native lowering.

## Commands And Queries

No recursive `.harness/worktrees` scan was run.

Project startup/context commands:

```sh
sed -n '1,220p' AGENTS.md
sed -n '1,260p' docs/PROGRESS.md
sed -n '1,260p' docs/ARCHITECTURE.md
sed -n '1,260p' docs/SUPPORT.md
sed -n '1,260p' README.md
sed -n '1,260p' docs/LOOP_MEMORY.md
sed -n '1,520p' docs/NEXT_TASKS.md
git status --short --branch
```

Harness SQLite MCP queries:

```sql
PRAGMA table_info(goals);
PRAGMA table_info(work_lanes);
PRAGMA table_info(agents);
PRAGMA table_info(events);
SELECT id, text, measure, status, plan_path, auditor_summary, created_at, updated_at FROM goals ORDER BY id DESC LIMIT 10;
SELECT id, ts, title, role, status, branch, worktree, expected_metric_delta, notes FROM work_lanes ORDER BY id DESC LIMIT 45;
SELECT id, name, role, current_status, worktree, branch, last_seen_at, ended_at, notes FROM agents WHERE name IN ('developer-233','developer-298','developer-427') ORDER BY id;
SELECT id, ts, type, agent_name, message, payload_json FROM events WHERE message LIKE '%developer-233%' OR message LIKE '%developer-298%' OR message LIKE '%lane 114%' OR message LIKE '%work_lanes#114%' OR message LIKE '%wordpress%' OR message LIKE '%database%' ORDER BY id DESC LIMIT 80;
```

Some later MCP read attempts against `messages` and broader database-related
lane searches returned `database is locked`; this report uses the successful
`work_lanes`, `agents`, and `events` evidence above plus direct targeted
worktree checks.

Targeted filesystem/git checks:

```sh
git status --short --branch
git remote -v
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-233 status --short --branch
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-233 diff --name-status
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-233 diff --stat
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-233 diff -- compiler/src/interpreter.rs compiler/tests/mysqli_extension.rs
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-233 status --porcelain=v1 --untracked-files=all
find /home/claude/php-to-native-compiler/.harness/worktrees/developer-233/tests/fixtures/milestone2305 -maxdepth 2 -type f -print
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-298 status --short --branch
git -C /home/claude/php-to-native-compiler/.harness/worktrees/developer-298 diff --name-status
git branch --all --list '*developer-233*' '*developer-298*' '*developer-427*'
git log --oneline --decorate --max-count=5 work/developer-233
git log --oneline --decorate --max-count=5 work/developer-298
sed -n '1,220p' docs/WORDPRESS_COMPATIBILITY.md
sed -n '1,220p' docs/extensions/mysqli.md
sed -n '1,160p' docs/extensions/pdo_mysql.md
```

Validation for this report:

```sh
git diff --check -- .harness/reports/wordpress-database-lane-quarantine-dev427.md
```
