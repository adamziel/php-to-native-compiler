name: Legacy STATUS dashboard disabled

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: legacy-status-gh-pages-disabled
  cancel-in-progress: false

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy STATUS publisher disabled; full PHPT dashboard loop owns gh-pages."
